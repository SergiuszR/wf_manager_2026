{
  "name": "webflow_2026-monorepo",
  "private": true,
  "scripts": {
    "vercel-build": "cd client && npm install && npm run build",
    "dev": "concurrently \"cd client && npm run dev\" \"cd server && npm run dev\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "spark-md5": "^3.0.2"
  },
  "devDependencies": {
    "@types/spark-md5": "^3.0.5",
    "concurrently": "^8.2.2"
  }
}
